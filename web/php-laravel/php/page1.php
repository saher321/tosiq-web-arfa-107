<?php
    echo "Hello from php" . "<br>";

    $a = 10;
    $b = 20;
    echo $a+$b;

    # sessions
    # basic programming (ifelse, loops, arrays)
    # Restful apis (mysql => xampp)
    # oop


    # $assignment = $a + $b - ($a * $b) / $c
?>

<!-- # Laravel
# Restful apis + Relations
# department + student -->